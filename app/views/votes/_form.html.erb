<%= simple_form_for(@vote) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :green %>
    <%= f.input :red %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
